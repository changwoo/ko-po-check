doc = ko-po-check
xml = $(doc).xml
entities = spelling-rule.xml

EXTRA_DIST = $(xml) $(entities)

CLEANFILES = $(doc) $(doc).junk

# all: 
# html: $(doc).xml $(entities)
# 	db2html -o ./$(doc) $(srcdir)/$(doc).xml

# distclean:
# 	rm -rf docbook2html-dir
# 	rm -rf ./$(doc) $(doc).junk
