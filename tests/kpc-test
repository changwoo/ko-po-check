#!/usr/bin/env python

import os,sys,glob

execfile = "python ../src/ko-po-check --moduledir=../src"
srcdir = os.getenv("srcdir")
testfiles = glob.glob("%s/test-[0-9]*.po" % srcdir)

testfiles.sort()

for file in testfiles:
    if (os.system(execfile + " " + file) != 0):
        sys.exit(1)
